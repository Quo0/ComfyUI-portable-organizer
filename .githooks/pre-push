#!/bin/sh
# See .githooks/commit-msg for why this is a wrapper and why a missing Node
# lets the push through. The refs being pushed arrive on stdin and are passed
# straight through.
command -v node >/dev/null 2>&1 || exit 0
exec node "$(dirname "$0")/../tools/hooks/pre-push.mjs" "$@"
