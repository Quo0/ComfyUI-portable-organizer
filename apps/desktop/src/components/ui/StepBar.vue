<script setup lang="ts">
// Ряд шага: заголовок экрана и действия в один ряд, вместо шапки —
// на линейных дорожках (мастер распаковки, добавление инстанса,
// редактор аргументов), где «Назад» — это шаг назад, а не уход в другой
// раздел, и стоит рядом с главным действием, а не у левого края экрана.
// Как и в ScreenHeader, состав ряда — забота вызывающего экрана: свой
// заголовок (`h2.title`), своя подпись, свои кнопки.
</script>

<template>
  <var class="StepBar">
    <div class="step-bar">
      <slot />
    </div>
  </var>
</template>

<style src="./StepBar.css" />
