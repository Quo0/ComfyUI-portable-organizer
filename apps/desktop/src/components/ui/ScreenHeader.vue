<script setup lang="ts">
// Шапка обычного экрана: заголовок и — по надобности — счётчик, поиск,
// сортировка, действия раздела в одном ряду с переносом. Слота под
// заголовок отдельно нет по той же причине, что и в Group: состав ряда
// свой на каждом экране (где-то просто h1, где-то ещё живой поиск),
// а `.screen-head` их не различает — это обычный flex-ряд.
//
// Шапка экрана вкладки инстанса (`InstanceView`, `InstanceTabView`)
// сюда пока не мигрирована: там `.screen-head` соседствует со вторым
// диалектом той же идеи (`.inst-toolbar`), и сливать их предстоит одним
// компонентом `InstanceHeader`, а не поодиночке.
</script>

<template>
  <var class="ScreenHeader">
    <header class="screen-head">
      <slot />
    </header>
  </var>
</template>

<style src="./ScreenHeader.css" />
