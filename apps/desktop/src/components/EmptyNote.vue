<script setup lang="ts">
// Строка «здесь пока пусто».
//
// Одно и то же сообщение на разных вкладках выглядело по-разному:
// где-то `hint` в одиннадцать пикселей, где-то `t-sm` в четырнадцать,
// где-то блок `.empty` с крупным текстом и отступом в двадцать четыре
// пикселя. Сравнить их между собой было нельзя — они на разных экранах, —
// и расхождение копилось молча. Теперь вид задан одним правилом `.blank`
// в дизайн-системе, а этот компонент его носит.
//
// Крупный `.empty` компонент не заменяет и не отменяет: у того другая
// работа — сказать, что делать дальше, заголовком, объяснением и кнопкой,
// и занимает он экран целиком. Здесь одна строка внутри отдела, у которого
// уже есть и заголовок, и кнопки.
//
// Текст приходит слотом, а не свойством: `t()` остаётся на месте вызова,
// рядом со своим экраном, и в строку при надобности встаёт ссылка.
</script>

<template>
  <var class="EmptyNote">
    <p class="blank"><slot /></p>
  </var>
</template>
