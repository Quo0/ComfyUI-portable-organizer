# Чеклист фаз

> Раздел плана. Оглавление — [PLAN.md](../PLAN.md).

---

Отмечаем по мере выполнения. Правки плана коммитим отдельно от кода.

## Фаза 0 — репозиторий и спайк

Цель фазы: доказать, что мультивебвью под `unstable` ведёт себя приемлемо и 403 не возникает. Здесь сосредоточен весь технический риск проекта — если упрёмся, переезжаем на Electron с `WebContentsView`, и вся остальная логика переносится один в один.

- [x] Создан `d:\projects\comfyui-portable-organizer`, `git init`, план лежит в репозитории
- [x] План перенесён в корень как `PLAN.md`, папка `docs/` освобождена под имя сайта
- [x] `pnpm-workspace.yaml`, корневой `package.json` со скриптами-оркестраторами, `.gitignore`
- [x] **Bundle identifier зафиксирован: `io.github.quo0.comfyui-organizer`.** Не менять никогда — от него зависит и путь к данным приложения, и их очистка при удалении
- [x] Первый коммит с планом
- [x] `apps/desktop`: скаффолд Tauri v2 + Vue 3 + TS + Vite, `tauri/unstable` в `Cargo.toml`
- [x] `vite.config.ts` по спецификации Tauri (strictPort 1420, `clearScreen: false`, target `chrome105`)
- [x] Спайк `tauri-specta`: **встало.** Команды и события описаны один раз на Rust, `src/bindings.ts` генерируется, Vue вызывает только его. Откат на ручные типы не нужен
- [x] Спавн `python.exe` по захардкоженному пути с `--disable-auto-launch --port 8188`
- [x] Браузер при этом **не открывается**
- [x] stderr стримится в `<pre>` живьём — первая строка через **1.6 с** после старта
- [x] Child webview на `127.0.0.1:8188` после готовности сервера — готовность за **54 с**
- [x] В консоли webview **нет 403** — вкладка отдала заголовок `ComfyUI`, то есть настоящую страницу
- [x] **Решение: продолжаем на Tauri.** Переезд на Electron не требуется

## Фаза 0.5 — каркас интерфейса

Идёт **до** экранов реестра: всё визуальное дальше строится на этом каркасе. Прикрутить i18n и токены темы потом — значит переписать все уже написанные экраны.

- [x] Оболочка приложения: рейл слева с пятью разделами, контентная область, `<RouterView>`
- [x] Сворачивание рейла до иконок, персистенс состояния
- [x] `styles/tokens.css` — копия `design/dist/tokens.css`, а не написанный заново файл. Все три состояния темы там уже собраны: светлая на `:root`, тёмная в `@media` под гардом `:root:not([data-theme="light"])` и в `:root[data-theme="dark"]`
- [x] Стор `ui`: тема, язык, очередь тостов; персистенс в `app_data_dir()`
- [x] Реакция на смену системной темы через `matchMedia`, без перезапуска
- [x] `window.set_theme()` — цвет системного титлбара следует за нашей темой
- [x] `vue-i18n` v11, `legacy: false`, локали `en` / `ru` / `zh-Hans` / `es`
- [x] Кастомные `pluralRules` для русского
- [x] `numberFormats` и `datetimeFormats` на локаль
- [x] Определение языка при первом запуске: `os::locale()` → `navigator.language` → `en`
- [x] Типизация ключей от `en.json` — неизвестный ключ ломает сборку
- [x] Скрипт `pnpm i18n:check` на паритет ключей; **шаг в CI отложен до Фазы 4** — самого CI ещё нет
- [x] Шрифтовый стек с покрытием CJK
- [x] `ToastHost`: успех с автоскрытием, ошибка с деталями и копированием, очередь и схлопывание повторов
- [x] Контракт `AppError { code, params }` в Rust; переводы ошибок только на фронте, неизвестный код показывается как код
- [x] Экран «Настройки → Внешний вид»

## Фаза 1 — реестр инстансов

- [x] `trait InstanceDiscovery` + `discovery/windows_portable.rs`
- [x] Валидация папки: `python_embeded\python.exe` + `ComfyUI\main.py`
- [x] Чтение версии из `ComfyUI\comfyui_version.py`, версия python
- [x] Персистенс в `app_data_dir()/instances.json` через `tauri-plugin-store`
- [x] `tauri.dev.conf.json` с отдельным идентификатором, подмешивается через `--config` в дев-скрипте
- [x] Проверено: дев-сборка и установленная используют разные папки данных — эксперименты не сносят рабочий реестр
- [x] `vue-router` со скелетом экранов; все строки экранов идут через `t()` с самого начала
- [x] Раздел «Инстансы»: список карточек и пустое состояние, ведущее в «Установку»
- [x] Сайдбар со списком инстансов и статусными точками
- [x] Переименование, смена акцентного цвета, удаление из реестра (папку не трогаем)

## Фаза 1.5 — мастер установки

После реестра, потому что мастер регистрирует инстансы, и до запуска: установка логически первее старта.

- [x] **Спайк:** `sevenz-rust2` на реальном 2.1 ГБ архиве, замер времени против 7-Zip, решение зафиксировано в разделе «Мастер установки»
- [x] `probe_archive`: записи, суммарный несжатый размер, единственная корневая папка
- [x] `extract` со срезом корня, verbatim-пути `\\?\`, стриминг прогресса
- [x] Временная `<dest>.cpo-partial` + `rename`; отмена и падение убирают её
- [x] `clone_tree` для второй и последующих целей, со стримингом прогресса
- [x] Проверка свободного места из заголовка архива, с учётом числа целей на одном томе
- [x] Предупреждение о длинном пути назначения
- [x] История архивов: проверка существования, размера и mtime, замена архива
- [x] Экран раздела «Установка»: две дорожки — существующая папка и мастер
- [x] Шаги мастера: архив → цели с именем, описанием и цветом → выполнение → итог
- [x] Поля `description` и `source` в реестре, показ источника в карточке инстанса

## Фаза 2 — запуск и жизненный цикл

- [x] **Стенд до супервизора:** `tools/fixtures/fake-instance/` — junction на системный Python, заглушка `main.py`, `.bat` включая `advanced\` с путями `..\`; скрипт создания, потому что junction не кладётся в git
- [x] Режимы стенда: обычный старт, долгий, падение, зависание, самоперезапуск, занятый порт
- [x] Отладка процессной части ведётся на стенде, а не на реальной сборке
- [x] Парсер `.bat`: пропуск `echo`/`pause`/`rem`, сбор `set KEY=VALUE`, токенизация с кавычками
- [x] Резолв относительных путей от директории `.bat` (проверить `advanced\*.bat` с `..\`)
- [x] Модель `LaunchProfile`, сидирование из `.bat`, фолбэк на `cmd /c` при провале парсинга
- [x] Мутация аргументов перед стартом: срезать `--port`, дописать `--port N --disable-auto-launch`
- [x] `ports.rs` — выдача свободного порта, уважение `preferredPort`
- [x] `trait ProcessSupervisor` + `supervise/windows.rs`
- [x] Job Object с `KILL_ON_JOB_CLOSE` — **но не через `CREATE_SUSPENDED`**, см. ниже
- [x] Кольцевой буфер логов (~5000 строк), чтение stdout **и** stderr в отдельных тредах
- [x] Обработка `\r` от tqdm: замена последней строки вместо добавления
- [x] Readiness-поллинг `GET /system_stats`, таймаут 5 минут
- [x] Стейт-машина `Stopped → Starting → Running → Stopping → Stopped | Crashed`
- [x] Остановка через `taskkill /PID /T /F` + ожидание освобождения порта
- [x] UI: сплит-кнопка «Запустить» с выпадашкой профилей, экран стартующего инстанса с консолью
- [x] Блок запущенных инстансов в рейле: переключение по клику, статус, бейдж при падении
- [ ] `hide()` / `show()` вебвью при уходе в другой раздел и возврате — **переехало в Фазу 3**: вебвью у инстанса появляется там, и прятать пока нечего

## Фаза 2.5 — общее хранилище моделей

Зависит от реестра (Фаза 1) и от мутации аргументов при старте (Фаза 2), но не зависит от встроенных вкладок. Поэтому вклинивается сюда, а нумерация остальных фаз не меняется.

- [x] `shared_models.rs`: `scan_root` — подпапки общего корня → канонические ключи, с учётом `unet` → `diffusion_models` и `clip` → `text_encoders`
- [x] Безусловный чёрный список `custom_nodes` + пояснение в UI, почему его нельзя шарить
- [x] Генератор YAML: секция `cpo_shared_<n>`, абсолютный `base_path`, прямые слэши, многострочные `|`, маркер-шапка для распознавания своего файла
- [x] Модель настроек: `roots[]` + `makeDefaultTarget`; на инстансе `{ enabled, applyMode }`
- [x] Режим «флаг»: генерация в `app_data_dir()`, дописывание `--extra-model-paths-config` рядом с `--port` в `profiles.rs`
- [x] Режим «файл в инстансе»: запись, распознавание своего файла по маркеру, бэкап чужого, экран подтверждения
- [x] Отключение общих моделей в обоих режимах, восстановление бэкапа
- [x] Проверка существования корня перед стартом + предупреждение «запустить без общих моделей»
- [x] Роут «Общие модели»: выбор папки, сводка, тумблер `is_default`, предпросмотр YAML, нераспознанные подпапки
- [x] Тумблер «Общие модели» на экране инстанса + значок в карточке
- [x] Шаг мастера установки «подключить к общей папке моделей», теми же компонентами, что в «Настройках»
- [x] Плашка «требуется рестарт» при смене настроек у работающего инстанса

## Фаза 2.6 — библиотека воркфлоу

- [x] `workflows.rs`: чтение папки библиотеки, `_library.json`, устойчивость к «файл без записи» и «запись без файла»
- [x] Настройка `workflowLibrary.path` + дефолт рядом с корнем общих моделей
- [x] Наполнение: кнопка «Добавить файл», drag&drop, забор из инстанса (`/v2/userdata` + `/userdata/{file}`)
- [x] Добавление в запущенный инстанс через `POST /userdata/…?overwrite=false`, обработка 409
- [x] Добавление в остановленный инстанс копией файла, **резолв `--user-directory` из аргументов профиля**
- [x] Диф нод: `nodes[].type` из JSON против `GET /object_info`; кэш снимка в `app_local_data_dir()` при каждом успешном старте
- [x] Роут «Библиотека»: поиск, теги, избранное, карточка, «Добавить в инстанс» со значком совместимости
- [x] Вкладка «Воркфлоу» на экране инстанса
- [x] Массовые операции в обе стороны
- [x] Шаг мастера установки «подключить к библиотеке воркфлоу»

## Фаза 3 — встроенные табы

- [ ] Создание child webview на инстанс, лейбл `comfy-<id>`
- [ ] `disable_drag_drop_handler()` — иначе ломается дроп на канвас
- [ ] Команда `set_webview_bounds` через `ResizeObserver` на контейнере контента; ресинк на ресайзе окна, сворачивании рейла и переходе по роуту
- [ ] Переключение табов через `show()` / `hide()`
- [ ] `on_navigation`: чужой хост → системный браузер, вернуть `false`
- [ ] `initialization_script` + capability для origin `http://127.0.0.1:*`
- [ ] Тулбар инстанса: Stop / Restart / Логи / Открыть в браузере / Папка output
- [ ] Аудит z-order: ни одной модалки или выпадашки поверх области контента

## Фаза 4 — полировка и упаковка

- [ ] Гард мульти-запуска: диалог про VRAM с выбором «остановить другой / запустить всё равно»
- [ ] Редактор кастомных аргументов, сохранение своих профилей
- [ ] Обработка самоперезапуска ComfyUI-Manager: 15 секунд опроса порта после выхода процесса
- [ ] Трей + `single-instance`, диалог при закрытии с работающими серверами
- [ ] Анализатор дубликатов моделей: отчёт «этот файл лежит в N инстансах, впустую X ГБ», сверка по имени и размеру (полный хэш для файлов по 20 ГБ неприемлем). **Только отчёт, без единого автоматического действия над файлами**
- [ ] Тест путей с пробелами и кириллицей
- [ ] Проверка скачиваний и «Сохранить изображение» из WebView2
- [ ] Импорт воркфлоу из PNG — ComfyUI кладёт граф в tEXt-чанк `workflow`
- [ ] Экран «О приложении»: пути данных, кнопки «открыть папку», что удалится при деинсталляции
- [ ] Поставить скил `tauri-v2` перед работой над capabilities, апдейтером и упаковкой: `npx skills add https://github.com/nodnarbnitram/claude-code-extensions --skill tauri-v2`. Раньше он бесполезен — про multiwebview, `unstable`, `tauri-specta` и Windows там ничего нет, а справки по capabilities, плагинам, апдейтеру и бандлингу как раз про эту фазу. Ставить настоящими файлами в `.claude/skills/`, как сделано с `vue`. Правила проекта имеют приоритет над его общими советами — в частности, про `async`-команды
- [ ] Сборка **NSIS `.exe`**, `installMode: currentUser`; MSI не собираем. Подпись кода не покупаем — вместо неё страница про SmartScreen
- [ ] Тест деинсталляции по чеклисту из «Верификации»
- [ ] Ключи апдейтера: `tauri signer generate`, публичный в `plugins.updater.pubkey`, приватный и пароль — в секреты CI. **Приватный ключ сохранить вне репозитория:** при утере обновления для установленных копий выпустить будет невозможно никогда
- [ ] `bundle.createUpdaterArtifacts: true` и `plugins.updater.endpoints` на `latest.json` в релизах
- [ ] Проверка перед установкой обновления: есть ли работающие инстансы, выбор «остановить и обновить» либо «при следующем запуске»
- [ ] Настройка «проверять обновления» с возможностью выключить
- [ ] `CHANGELOG.md`; тело релиза собирается из его секции
- [ ] `release-desktop.yml`: сверка тега с версией → ворота качества → `tauri-action` черновиком → SHA-256 в тело → публикация
- [ ] Предрелизы по тегам с дефисом
- [ ] `.github/workflows/release-desktop.yml` — сборка по тегу, триггер по путям `apps/desktop/**`

## Фаза 5 — сайт документации

Независима от Фаз 2-4 и может идти параллельно. Единственная связь — скриншот для лендинга, он появляется после Фазы 3.

- [ ] `apps/docs`: скаффолд VitePress, `.vitepress/config.ts` с `base` под GitHub Pages
- [ ] Настроить `locales` и пути `/ru/`, `/zh/`, `/es/` **сразу**, контент писать только английский — переводы добавятся позже без ломки ссылок
- [ ] Лендинг: что это, скриншот окна со встроенной вкладкой ComfyUI, кнопка скачивания
- [ ] `guide/`: установка, добавление портабл-инстанса, профили запуска, порты и конфликты, известные ограничения (самоперезапуск ComfyUI-Manager, VRAM при двух инстансах)
- [ ] `guide/shared-models`: как разложить общую папку, что даёт `is_default`, чем отличаются два режима применения, почему `custom_nodes` шарить нельзя
- [ ] `guide/install`: где взять архив, чем мастер отличается от добавления существующей папки, почему стоит выбирать короткий путь
- [ ] `guide/workflows`: библиотека воркфлоу, что означает предупреждение о недостающих нодах
- [ ] `guide/uninstall`: что приложение хранит и где, что удаляется галкой при деинсталляции, а что остаётся (модели, библиотека, файлы, записанные в папки инстансов)
- [ ] `reference/`: формат `instances.json`, поля `LaunchProfile`, добавляемые нами флаги (`--port`, `--disable-auto-launch`, `--extra-model-paths-config`) и почему, формат генерируемого YAML
- [ ] `download.data.ts` — загрузчик данных о последнем релизе на этапе сборки, с запасным вариантом при недоступности GitHub API
- [ ] Страница скачивания: кнопка, версия, дата, размер, контрольная сумма, ссылка на все версии
- [ ] `guide/smartscreen`: как выглядит предупреждение, почему появляется, что нажать, как сверить сумму — ссылка стоит рядом с кнопкой, а не в глубине доков
- [ ] Пересборка сайта по событию публикации релиза
- [ ] `.github/workflows/deploy-docs.yml` → GitHub Pages, триггер только по путям `apps/docs/**`
