packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 11 does not run dependencies' postinstall scripts until they are allowed
# by name. Vite needs esbuild: its script unpacks the native binary without
# which the dev server will not start.
allowBuilds:
  esbuild: true
