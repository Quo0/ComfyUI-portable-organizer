packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 11 не запускает postinstall-скрипты зависимостей, пока их не разрешат
# поимённо. esbuild нужен Vite: его скрипт распаковывает нативный бинарь,
# без которого дев-сервер не поднимется.
allowBuilds:
  esbuild: true
